# GBLOCK V1 Status - 2026-09-24

Status: ROADMAP UPDATE

## Completed Packets

- `GBLOCK-001`: project bootstrap, upstream research, license/build/source comparison.
- `GBLOCK-002`: pure Kotlin game core and gesture-command prototype.
- `GBLOCK-003`: minimal Android Compose renderer shell.
- `GBLOCK-004`: V1 roadmap and packet graph.
- `GBLOCK-005`: Android gesture input integration.
- `GBLOCK-006`: gravity loop and session state.
- `GBLOCK-007`: prototype UX layout and minimal pause/restart menu.
- `GBLOCK-011`: audio boundary and original placeholder audio.
  - Generated PCM placeholder cues added behind an Android audio boundary.
  - Runtime `AUDIO` and `MUSIC` toggles added.
  - No commercial soundtrack files or proprietary samples imported.
- `GBLOCK-010`: persistence/settings/high scores.
  - High score, skin, sensitivity, audio, music, and app volume persist locally.
  - `RESET HIGH` clear behavior added.
  - App-private `shared_prefs/gblock_preferences.xml` confirmed on Zebra TC57.
- `GBLOCK-013`: build tooling and source hygiene.
  - Project-wide concise code comment pass completed.
  - Source inventory, warning register, ignored/generated file posture, upstream isolation, and build command documented.
- `GBLOCK-009`: original retro presentation baseline.
  - Default/reference skin is `GBLOCK DEFAULT`.
  - Bundled skin model includes `SkinPalette` and `SkinLayout`.
  - Included skins can change colors and simple screen arrangement values inside the APK.
  - `HANDHELD` monochrome private-use reference skin added.
  - Game Boy-style side layout added: playfield left, status/next/settings column right.

## In-Progress Packets

- `GBLOCK-008`: device gesture calibration.
  - Zebra TC57 target recorded.
  - APK installs and launches.
  - First human feedback recorded: pause hard to trigger; hard drop accidental.
  - Thresholds tuned and reinstalled.
  - Retest still required before locking V1 defaults.

## Not Yet Started

- `GBLOCK-012`: V1 test and regression harness.
- `GBLOCK-014`: licensing/privacy/accessibility review.
- `GBLOCK-015`: V1 release candidate acceptance gate.

## V1 Distance Estimate

Rough status: about 75-80 percent of V1 implementation/research path is complete.

This estimate is based on packet count and risk, not just code volume. The core game, gesture command boundary, Android shell, loop, settings, persistence, bundled skin model, side-panel layout, audio boundary, source comments, and source hygiene register exist. Remaining V1 risk is concentrated in device-feel calibration, regression coverage, final visual confirmation on a good test device, and final audit/gate work.

## Next Required Evidence

- Retest tuned gestures on the Zebra TC57:
  - two-finger pause should be easier.
  - hard drop should require a more deliberate flick.
  - normal soft drop should not accidentally hard drop.
- Unlock TC57 and visually confirm:
  - Game Boy-style layout appears correctly with playfield left and stats/next/settings on the right.
  - `SET` menu opens.
  - sensitivity presets can be selected.
  - skin dropdown can switch between `GBLOCK DEFAULT` and `HANDHELD`.
  - handheld skin remains readable.
  - generated PCM audio/music on a device with working audio hardware.

## Current V1 Blockers

- Gesture defaults are not locked.
- Manual audio listening confirmation is blocked on the current Zebra TC57 because YouTube is also silent on that device.
- V1 regression harness is incomplete.
- Final license/privacy/accessibility review is not done.
- No final acceptance report exists.
